{
  "name": "quran-project",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@8.15.9",
  "scripts": {
    "format:check": "pnpm -r --if-present format:check",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
